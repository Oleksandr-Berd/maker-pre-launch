export type IntroStyle = {
  bg: string | null;
  bg2?: string | null;
  bg3?: string | null;
};

export type FormType = {
    formSubmit:(email:string) => void
}

export type InputStyle = {
    status: string,
}